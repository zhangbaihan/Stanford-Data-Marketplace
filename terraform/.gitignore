# Terraform state files (contain sensitive data)
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# Terraform directory (downloaded providers and modules)
.terraform/

# Lock file (can be committed, but regenerated easily)
.terraform.lock.hcl

# Variable files that may contain secrets
*.tfvars
*.tfvars.json

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
